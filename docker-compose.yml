services:
  app:
    build: .
    env_file: .env                       # secrets come from your local .env, never from the image
    environment:
      DATABASE_PATH: /data/remediations.db
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data                     # SQLite state survives restarts and rebuilds
    restart: unless-stopped
